name: Build Windows Installer

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # Instalar dependencias (usa bash en lugar de PowerShell)
      - name: Install dependencies
        shell: bash
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      # Construir instalador (asegúrate de tener script "dist" en package.json)
      - name: Build installer
        run: npm run dist

      # Subir artefactos generados
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SysmeLite-installer
          path: dist/**
